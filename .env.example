# FPL API Configuration
# Used in development and production

# Local development: Vite serves the internal API in-process.
# Production: Cloudflare Pages Functions serves the same-origin API.
# Leave this as /api/fpl unless intentionally hosting the API elsewhere.
VITE_FPL_API_BASE_URL=/api/fpl
